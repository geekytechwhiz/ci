# SSM contract consumed by the application stack.
# Data Stack publishes TABLE_NAME, TABLE_ARN, STREAM_ARN.
# Infrastructure stack publishes SQS_QUEUE_URL, SQS_QUEUE_ARN.
prefix: /${self:provider.stage}/workflow-service

required:
  - TABLE_NAME
  - TABLE_ARN
  - STREAM_ARN
  - SQS_QUEUE_URL
  - SQS_QUEUE_ARN
